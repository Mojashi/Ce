VerGoにおける構造体
・複数のboolを意味づけ（名前付け）してまとめて保持できる
・メソッドが利用でき、コードが簡潔になり意味の把握が容易になる


・必要条件のみを考慮できる文法
実際にShortestPathのアルゴリズムをここに展開する必要はなく、今欲しい性質（ここでは全てのパスより長さが短い事）のみを利用できる。
ShortestPath(g,s,t) : x => {
    return g.getPath(s,t).cost() < x;
}